const Discord = require("discord.js");


module.exports.run = async (bot, message, args) => {
    message.delete();
  let botmessage = args.join(" ");
/*
if (message.channel.id == '604286086256263194') {
  
    bot.guilds.forEach(guild => {
      
    setTimeout(() => {
        guild.members.forEach(user => {
          user.send(botmessage);
          setTimeout(() => {
            console.error(`MP envoyé par ${message.author.username} dans ${guild.name}`).catch(err => console.log(err));
          }, 5000); 
        });
    }, 5000); 
      
    });
  

  
  
  } else {
    message.delete();
  }
*/  
}
  
  



// <a:plus:603929599801425921>
